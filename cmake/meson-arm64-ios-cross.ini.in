# Meson cross file: build DXVK for iOS (arm64, device) on a macOS host.
# Mirrors meson-arm64-native.ini but targets the iPhoneOS SDK.
# SDL3 for iOS must be discoverable via PKG_CONFIG_PATH (build/ios/sdl3-install/lib/pkgconfig).

[binaries]
c = 'clang'
cpp = 'clang++'
objc = 'clang'
objcpp = 'clang++'
ar = 'ar'
strip = 'strip'
pkg-config = 'pkg-config'

[host_machine]
system = 'darwin'
cpu_family = 'aarch64'
cpu = 'arm64'
endian = 'little'

[built-in options]
# GeneralsX @build dvorovrus 26/09/2026 Expose LunarG Vulkan headers to the
# iOS DXVK Meson compiler checks; VULKAN_SDK alone is not consumed as an
# include path by Meson during this cross build.
c_args = ['-arch', 'arm64', '-isysroot', '@IOS_SDK@', '-miphoneos-version-min=16.0', '-I@VULKAN_SDK_ENV@/include']
cpp_args = ['-arch', 'arm64', '-isysroot', '@IOS_SDK@', '-miphoneos-version-min=16.0', '-I@VULKAN_SDK_ENV@/include']
c_link_args = ['-arch', 'arm64', '-isysroot', '@IOS_SDK@', '-miphoneos-version-min=16.0']
cpp_link_args = ['-arch', 'arm64', '-isysroot', '@IOS_SDK@', '-miphoneos-version-min=16.0']
